services:
  portal:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - VITE_API_URL=http://127.0.0.1:3000
    command: npm run dev -- --host 0.0.0.0
